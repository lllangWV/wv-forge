[workspace]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["linux-64"]
preview = ["pixi-build"]

[system-requirements]
cuda = "12.9"

[dependencies]
pytorch3d = { path = "recipe" }
python = "3.12.*"
cuda-version = ">=12.6.0"
pytorch-gpu = ">=2.8.0,<2.10.0"




[package]
name = "pytorch3d"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "0.3.*" }