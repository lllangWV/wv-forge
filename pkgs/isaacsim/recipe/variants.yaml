# Isaac Sim requires GCC 11 — the Kit SDK headers contain implicit pointer
# conversions that are errors in GCC 14 but accepted by GCC 11.
c_compiler_version:
  - "11"
cxx_compiler_version:
  - "11"

# Isaac Sim's bundled extensions link against libxml2.so.2 (SONAME 2).
# conda-forge changed the SONAME to libxml2.so.16 starting with libxml2 2.13.
# Pin to the last version that provides the old SONAME.
libxml2:
  - "2.12"
